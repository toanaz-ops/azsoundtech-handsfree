# Review Package - Task 3

## Commit Range
Base: 69d61e5b72e95aee586975a7006adcba34216edc
Head: 510f93d904b44fa0f2ea8b6614a933f9ad9b6d5a

## Commit Log
510f93d feat: add minimal JUCE application stub

## Diff Stats
 CMakeLists.txt            |  6 +++++  src/app/MainComponent.cpp | 25 +++++++++++++++++  src/app/MainComponent.h   | 16 +++++++++++  src/main.cpp              | 68 +++++++++++++++++++++++++++++++++++++++++++++++  4 files changed, 115 insertions(+)

## Full Diff
diff --git a/CMakeLists.txt b/CMakeLists.txt index 745cf06..08bb183 100644 --- a/CMakeLists.txt +++ b/CMakeLists.txt @@ -23,20 +23,26 @@ endif()  juce_add_gui_app(HandsFree      PRODUCT_NAME "AZ Soundtech Hands-free"      COMPANY_NAME "AZ Soundtech"      BUNDLE_ID "com.azsoundtech.handsfree"      VERSION "1.0.0"  )    # Source files  target_sources(HandsFree PRIVATE      src/main.cpp +    src/app/MainComponent.cpp +    src/app/MainComponent.h +) + +target_include_directories(HandsFree PRIVATE +    ${CMAKE_SOURCE_DIR}/src  )    # Link JUCE modules  target_link_libraries(HandsFree      PRIVATE          juce::juce_audio_devices          juce::juce_audio_utils          juce::juce_dsp          juce::juce_gui_basics          juce::juce_recommended_config_flags diff --git a/src/app/MainComponent.cpp b/src/app/MainComponent.cpp new file mode 100644 index 0000000..d237d59 --- /dev/null +++ b/src/app/MainComponent.cpp @@ -0,0 +1,25 @@ +#include "MainComponent.h" + +MainComponent::MainComponent() +{ +    setSize(800, 600); +} + +MainComponent::~MainComponent() +{ +} + +void MainComponent::paint(juce::Graphics& g) +{ +    g.fillAll(getLookAndFeel().findColour(juce::ResizableWindow::backgroundColourId)); + +    g.setColour(juce::Colours::white); +    g.setFont(20.0f); +    g.drawText("AZ Soundtech Hands-free", getLocalBounds(), +               juce::Justification::centred, true); +} + +void MainComponent::resized() +{ +    // Future components will be laid out here +} diff --git a/src/app/MainComponent.h b/src/app/MainComponent.h new file mode 100644 index 0000000..afad91b --- /dev/null +++ b/src/app/MainComponent.h @@ -0,0 +1,16 @@ +#pragma once + +#include <JuceHeader.h> + +class MainComponent : public juce::Component +{ +public: +    MainComponent(); +    ~MainComponent() override; + +    void paint(juce::Graphics& g) override; +    void resized() override; + +private: +    JUCE_DECLARE_NON_COPYABLE_WITH_LEAK_DETECTOR(MainComponent) +}; diff --git a/src/main.cpp b/src/main.cpp new file mode 100644 index 0000000..73a035d --- /dev/null +++ b/src/main.cpp @@ -0,0 +1,68 @@ +#include <JuceHeader.h> +#include "app/MainComponent.h" + +class HandsFreeApplication : public juce::JUCEApplication +{ +public: +    HandsFreeApplication() = default; + +    const juce::String getApplicationName() override +    { +        return "AZ Soundtech Hands-free"; +    } + +    const juce::String getApplicationVersion() override +    { +        return "1.0.0"; +    } + +    bool moreThanOneInstanceAllowed() override +    { +        return false; +    } + +    void initialise(const juce::String&) override +    { +        mainWindow = std::make_unique<MainWindow>(getApplicationName()); +    } + +    void shutdown() override +    { +        mainWindow.reset(); +    } + +    void systemRequestedQuit() override +    { +        quit(); +    } + +private: +    class MainWindow : public juce::DocumentWindow +    { +    public: +        MainWindow(const juce::String& name) +            : DocumentWindow(name, +                             juce::Desktop::getInstance().getDefaultLookAndFeel() +                                 .findColour(juce::ResizableWindow::backgroundColourId), +                             DocumentWindow::allButtons) +        { +            setUsingNativeTitleBar(true); +            setContentOwned(new MainComponent(), true); +            setResizable(true, true); +            centreWithSize(getWidth(), getHeight()); +            setVisible(true); +        } + +        void closeButtonPressed() override +        { +            juce::JUCEApplication::getInstance()->systemRequestedQuit(); +        } + +    private: +        JUCE_DECLARE_NON_COPYABLE_WITH_LEAK_DETECTOR(MainWindow) +    }; + +    std::unique_ptr<MainWindow> mainWindow; +}; + +START_JUCE_APPLICATION(HandsFreeApplication)

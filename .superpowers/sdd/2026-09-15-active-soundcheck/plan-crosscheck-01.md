# Plan cross-check 01 — read-only, opus, against b5116e9 (2026-09-15)

## BLOCKER
B-1. copySnapshot() never refreshes headless (latest_.notches written only in runOnce() drain loop, NotchController.cpp:550-560, list :568-582, publish :617-650). Task 4 SnapshotCarriesOrigin and Task 7 NotchRig tests read a permanently empty snapshot -> pump 512 floats into the tap(s) before runOnce() (precedent tests/test_gui_wiring.cpp:1033-1035; memory preset-save-roundtrip). PRODUCTION: applySoundcheckResults re-reads copySnapshot before each setNotch in a tight loop on the message thread; snapshot refreshes at hop cadence (~10.7 ms) on the detector thread, so all six reads return the same frame, firstFreeIndexTopDown returns 15 every time, setNotchImpl (:226-245) overwrites without n.active check -> 5 of 6 proposals lost. FIX: (a) local std::array<bool, kSlots*2> takenThisCall seeded from one snapshot at entry, marked per index handed out; firstFreeIndexTopDown takes it; keep the re-read as a detector guard only. (b) LinkedSlotPlacesBothLanesAtOneIndex: snap.laneCount stale default 1 (NotchController.h:412) -> lane count from controller.effectiveLinked()/explicit param or pump both taps. (c) every snapshot-reading test pumps 512 floats (both taps for stereo) before runOnce().
B-2. Recorder (tests/test_notchcontroller.cpp:1471-1486) has no operator(); use rec.sink(). Must be declared BEFORE the Harness (:1466-1470, dtor flush). It lives in that TU's anonymous namespace -> not visible in test_soundcheckcontroller.cpp: define a local recorder there (before NotchRig) or promote Recorder into tests/test_gui_helpers.h.
B-3. makeClearEvent does not exist anywhere; notchEventToVarForTest (MainComponent.h:194) is used only from tests/test_gui_wiring.cpp:1337,1360,1365,1383. Put the SoundcheckReplace reason test in test_gui_wiring.cpp beside SetAndClearKeepTheirOwnEventNames (:1356-1369), build the event inline (kind=Clear, reason=SoundcheckReplace, expect "soundcheck_replace"). Delete the SessionLoggerSoundcheck variant; fix Task 4 file list/git add.
B-4. AudioEngine::isRunning() is only set by start() (AudioEngine.cpp:86); audioDeviceAboutToStart(nullptr) does not set it (:686-739); numInput/OutputChannels_ written only in the callback (:506-507; test_audioengine.cpp:409-410). So Refuses* tests get EngineNotRunning and every armed run aborts engine_stopped. FIX: test seam void setRunningForTest(bool) beside setSoundcheckPeakUnclampedForTest; Rig ctor calls it AND drives one block() before preflight(); replace every r.engine.audioDeviceAboutToStart(nullptr) line in Task 6; RefusesWhenEngineNotRunning keeps it false.
B-5. OutputClampStillCoversTheSweepPath: peak seam bypasses the setter clamp but SoundcheckSignal ctor clamps (plan 623) and sampleAt clamps (653-654) -> |v|<=0.1 always, sawSomething never set. FIX: seam past the signal: setSoundcheckGainUnclampedForTest(float gain) (default 1.0f) applied at point 3 as out[n] += v * scGainUnclamped; test sets 50.0f. Drop the peak seam unless used elsewhere.
B-6. Insertion point 1 sets capSource inside the per-lane loop, only if an enabled lane has inIdx == scCaptureInChannel_. NoiseFloorCapturesWithoutEmitting (routeMono 0,0,0 then capture channel 1) and DeviceRestartDrainsTheCaptureRing capture nothing. FIX (production): hoist next to the bounds check: const float* capSource = (scInCh >= 0 && inputChannelData != nullptr) ? inputChannelData[scInCh] : nullptr; drop point 1 from the lane loop.
B-7. Task 3 AtMostSixPerLaneAndTheHottestSurvive: marginDb = -hDb so sort by hDb desc => marginDb ascending. EXPECT_LE(out.candidates[0].marginDb, out.candidates[1].marginDb); ASSERT_LE(marginDb, -5.0f).
B-8. Task 1 InstantaneousFrequencyIsMonotoneAndHitsBothEnds: f(n)=100*100^(n/T), T=144000: atStart centre 2400 -> 107.98 Hz (tol 5 fails); atEnd centre 141600 -> 9263 Hz (tol 500 fails). FIX: probe true extremes and compare against the closed form computed in the test (spanA = 0.020*sr, spanB = 0.004*sr; cA = spanA/2+1, cB = total - spanB/2 - 1; EXPECT_NEAR(measured, closedForm(c), closedForm(c)*0.05)). Keep the monotone loop.
B-9. gui::ModeRail::Orientation::vertical -> Orientation::Vertical (ModeRail.h:23; tests/test_moderail.cpp:28,41). Three tests.
B-10. evOf() returns const char* from toRawUTF8() of a temporary juce::String -> dangling. Return juce::String; drop juce::String(...) wrappers at call sites (plan 2914, 3057, 3142, 3197, 4024).

## IMPORTANT
I-1. ResultsTimeoutIsTwentySeconds: run = 2x4.5 s = 9000 ms; pump(12000) already 3000 ms into Results; pump(19000) -> 22000 > 20000 -> Idle. FIX: ASSERT Results, then advance kResultsTimeoutMs-1000 (still Results), then +2000 (Idle).
I-2. DecayLongerThanTheTailIsUnderRead: the plan's own derivation gives 0.035 dB shortfall, not 3.0; in the fixture the sweep crosses 1 kHz at t=1.5 s so t60=2.0 leaves ~0 lost. FIX: t60 = 8.0, tails 0.7 vs 12.0 -> lost ~2.6 dB (put the algebra in the comment), or drop EXPECT_NEAR and keep ordering + bound. Do not ship 3.0.
I-3. SpectrumView::setSoundcheckOverlay needs per-bin marked[] but OutputResult has only markedCount (plan 2586-2598) while SoundcheckCandidates::Output has marked (1243). Add std::array<bool, LoopGainEstimator::kNumBins> marked{} to OutputResult; Task 6 Step 4 states analyseCurrentTarget() copies marked and negates hDb into marginDb.
I-4. copyResultsForSlot consumed in Task 10 (4636) but produced by no task. Add [[nodiscard]] std::vector<OutputResult> copyResultsForSlot(int slot) const to Task 6 Interfaces + private state.
I-5. MainComponent accessors: existing getNotchControllerForTest (:188), getAudioEngine (:67), loadPreset (:89), showMessage (:118), notchEventToVarForTest (:194). NOT existing: getDevicePanelForTest, getModeRailForTest, lastMessageForTest, setSoundcheckControlsLockedForTest, getSoundcheckControllerForTest. State them in Task 10 Interfaces as new TEST ACCESSOR members modelled on getSlotPanelForTest (:176)/getSpectrumViewForTest (:182); lastMessageForTest() returns panelMessage_ (MainComponent.cpp:688-692).
I-6. RoomMemoryIsUntouched cannot go red: the Soundcheck room-memory gate is at NotchController.cpp:1169-1170 inside placeConfirmed (NOT :1116 as plan says at 169, 3220 and inv 18); test only calls setNotch/clearNotch which never touch roomMemory_ (auto-release path :360-405). FIX: drive a real detector placement (probeDepthAt family tests/test_notchcontroller.cpp:378+) or retitle honestly and note inv 18 is covered by lane G's SoundcheckPlacesAtTheFullSliderDepth / SoundcheckNotchesNeverDeepen (:1164-1165). Correct :1116 -> :1169 in all three places.
I-7. Task 8: EveryAbortReasonHasItsOwnName builds a set of 8 string literals, never calls abortReasonName -> expose abortReasonName (or abortReasonNameForTest), loop the 8 enumerators, assert size 8 and no "unknown". RingRiskIsNullWhenInvalid checks the JSON writer not the controller -> delete (RefusesWhenRingRiskIsRising already covers). Includes: <set>, <algorithm>.
I-8. roundToThreeSignificantFiguresForTest: make it a static member of SoundcheckController forwarding to file-local round3sf; call qualified; include app/SoundcheckController.h in the test.
I-9. Point 3 reads currentSampleRate_ mid-callback (plan 2360) against "nothing below reads the atomics again" (2312). Snapshot it beside bypass as scSampleRate; amend inv 7 wording ("seven atomics plus the sample rate").
I-10. setDetectionActiveOnAllSlots is a bare public std::function read from the lane M thread; assignment concurrent with invocation = data race. No simpler construction keeps inv 12 + inv 17 (setDetectionActive is one relaxed store, NotchController.cpp:936-939). FIX: header contract "assigned once before start(), never after; abortAndJoin()/stop() must have returned before reassign/destroy"; Task 10 Step 2 assigns it in MainComponent ctor before any soundcheck_.start().
I-11. DelayDoesNotChangeTheAnswer: flatRoom(x, 900, 0.5, 1.6) renders 4.6 s; delayed sweep ends 3.9 s -> nothing truncated. FIX: flatRoom(x, 900.0, 0.5, 0.7) (real kTailSeconds); assert within 3 dB of -6.02 while the quick case is within 1 dB.
I-12. applySoundcheckResults step (b) clears every Soundcheck-origin notch in the slot's snapshot (both lanes) while the comment says "this lane's". Restrict to n.channel present in results (fix comment to "this slot's") or state that a re-run replaces the whole slot; ARerunReplacesItsOwnPreviousProposals is mono and cannot tell.

## MINOR
m-2 reasonName PartialApplyUnwind anchor MainComponent.cpp:75 (not 74). m-3 kTapSilenceTimeoutMs NotchController.h:83 (not 82). m-4 room-memory gate :1169 (see I-6). m-5 detectionActiveForTest NotchController.h:388 (not 404-405). m-6 runOnce NotchController.h:350. m-7 getSoundcheckRemainingMs .cpp:1030; startSoundcheck :1005. m-8 getAvailableRead LockFreeRingBuffer.h:115. m-9 logstats.py encoding :19; if/elif :46-87; fall-through comment :62-66; reason column :132. m-10 dashedStemPathElementCountForTest SpectrumView.h:145, kDashedStemReserveFloats :185. m-11 tools/snapshot.cpp has no scene registry: main() at :156 is linear shoot() calls (:212, :357) -> "a third shoot() block". m-12 fixture: session_end is the last line (tests/fixtures/session-sample.jsonl:15, t=60000) -> insert BEFORE it. m-13 Task 5 adds 10 tests (not 11). m-14 Task 6 defines 21 (not 20). m-15 Task 9 defines 8 (not 9). m-16 RefusesWithZeroChannels builds RunParams that preflight does not take -> delete dead code. m-17 UntrustedBinIsNeverACandidate pokes 8 kHz > kTrustedHighHz 6000 -> use a bin <= 6 kHz with trust=0. m-18 kSweepSeconds*0.0+0.7 -> 0.7. m-19 SoundcheckCandidates::smooth "exposed for tests" but untested. m-20 #include dsp/SoundcheckSignal.h only in AudioEngine.cpp. m-21 includes: Task 7 <thread>, <memory>; Task 5 <thread>/<atomic>. m-22 two identical Candidate types (SoundcheckCandidates::Candidate, OutputResult::Candidate) with no stated conversion -> Task 6 Step 4 copies field-by-field.

## Also
- NoiseFloorOfAQuietRoomDoesNotAbort: 7.35 is a 60-seed one-shot max; dense sampling crossed 10 once (13.99, memory peakiness-sweep-2048). Max over ~1023 bins of a looped 16384-sample buffer = flake. FIX: assert against params(20.0f) here (gate 10 covered by NoiseFloorGateIsReadAtArm) or pin ASSERT_LT(worstPeakinessForTest(), 10.0f) first.
- HotMicAbortsOnlyAfterTheHold never proves the hold: add one 5.33 ms block at 0.9 then quiet -> assert NO abort, before the long burst.
- Task 11: roadmap lane A row "chờ M" is line 74 — add the number.
- Corrected running test estimates: 554 / 562 / 574 / 577 / 587 / 608 / 619 / 625 / 633 / 638 (baseline 547 reproduced: 546 TEST macros + logstats_fixture).
- Verified clean: thread/lock audit of points 3/4 (except I-9); DSP hygiene; failNextSetNotchOnLaneForTest one-shot (NotchController.h:298, .cpp:216-220); TapsStaySuspendedAcrossTheGap seam valid; §4.4 arithmetic and all constants.

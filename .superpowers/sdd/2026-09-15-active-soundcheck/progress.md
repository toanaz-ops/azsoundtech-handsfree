# SDD ledger — plan: docs/superpowers/plans/2026-09-15-active-soundcheck.md

Baseline: main a6be099 → branch feat/lane-m-active-soundcheck, worktree build 547/547 (2026-09-15, submodules re-cloned, JUCE_ASIO=1). Spec rev 4 2c84075, plan rev 2 335b855 (cross-check 01 absorbed).
Task 1: dispatched implementer (sonnet) at BASE 335b855, brief task-1-brief.md, 2026-09-15 22:42. Plan rev 2 scoped re-check (opus read-only) running in parallel.
Task 1: implementer DONE 164092e (554/554, 7 new; concerns: cross-worktree Write tool sandbox → cp from scratchpad; 'ahead by 1' stale ref). Plan rev 3 9069245 landed in between (docs only). Reviewer (sonnet) dispatched on review-335b855..164092e.diff
Task 1: review (sonnet) spec ✅ quality Approved, 0 findings (math re-derived independently; double clamp is two independent enforcements; rampOut exact-zero branch).
Task 1: complete (commits 335b855..164092e, review clean)
Task 2: dispatched implementer (opus) at BASE 164092e, brief task-2-brief.md
Task 2: implementer DONE_WITH_CONCERNS 6dcbce0 (562/562, +8; C1 brief's 2.62 dB misevaluated → 0.0983 dB at t60=8 (kept fixture, changed number, +control case); C2 NoiseFloorIsSubtracted sigma raised so mutation dies; C3 DelayDoesNotChangeTheAnswer still no truncation in measured band; C4 reset() untested; C5 model opus not fable). Reviewer (opus) dispatched on review-164092e..6dcbce0.diff
Task 2: review (opus) spec COMPLIANT (C1/C2 deviations independently confirmed correct), quality sound; 2 Important plan-mandated: (I1) trusted[] ignores band-SNR gate — measured==false yet trusted[k]==true possible (LoopGainEstimator.cpp:139-140 vs :152-153), spec §4.11 says band SNR<12 ⇒ nothing trusted; (I2) DelayDoesNotChangeTheAnswer comment claims truncation that happens only at 7.35–10 kHz, outside the [200 Hz,4 kHz] band measured; 3 dB tolerance decorative. Controller ruling: both fixes implement the SPEC's stated intent (band gate = nothing trusted; a delay test whose "reads low" clause is real) — the plan text was the defect, not the spec; no owner call needed. Fix round 1/5 (resume implementer opus): I1 fill(false) when !measured + a test that separates the two gates; I2 second mean over 7.4–10 kHz with the "allowed to read low" assertion on THAT band, mid-band tolerance tightened to 0.5 dB; minors 3,4,5,6,7 folded.
Task 2: fix round 1/5 — implementer DONE 327762b (564/564, +2: NotMeasuredClearsEveryTrustedBin, ResetClearsEveryStream; high-band delay assertion slow < quick-40 measured 130 dB drop). Re-review (sonnet) dispatched on review-6dcbce0..327762b.diff
Task 2: fix round 1/5 (7 addressed, 0 open; commits 6dcbce0..327762b)
Task 2: minor (deferred): NotMeasuredClearsEveryTrustedBin proves "bins the bin-gate alone would trust" only via report calibration, not an in-test assertion; bandSnrDb now load-bearing in 3 fixtures (shared calibration fragility).
Task 2: complete (commits 164092e..327762b, review clean after 1 fix round)
Task 3: dispatched implementer (opus) at BASE 327762b, brief task-3-brief.md
Task 3: implementer DONE_WITH_CONCERNS 119b033 (577/577, +13: 12 brief + AboveTheTrustedBandIsNeverACandidate because band filter removal left all 12 green; 12 mutations killed; concerns: saturatedBins counts survivors only (Task 9 GUI string), Input::ceilingDb default 0.0 = no cut (Task 6 must set), float Candidate vs double depthFor). Reviewer (opus) dispatched on review-327762b..119b033.diff
Task 3: review (opus) spec COMPLIANT (six examples re-derived exact; 13th test justified); 3 Important: (I1) Ladder unvalidated — empty ladder ⇒ every proposal −24, null+count ⇒ deref; (I2) step-6 `depthDb < 0` sentinel has zero coverage (all fixtures ceiling −24); (I3 plan-mandated) ceilingDb default 0.0 is the safe direction but a forgotten ceiling is indistinguishable from "room clean". Controller ruling on I3: spec says ceiling is DATA — make "unset" detectable without changing the safe value: NaN sentinel default, pick() refuses proposals (marks still) and sets Output::ceilingMissing; Task 6 must never show "no candidates" without distinguishing. Minors: M-2 NaN hDb ⇒ −24 with saturated=false (CLAUDE.md: NaN guards go IN) → fix; M-1/M-3/M-5 comments; M-4 deferred. Fix round 1/5 (resume implementer opus).
Task 3: minor (deferred): M-4 hzToBin recomputed per bin × live notch; band edge differs by one bin from estimator (M-5, comment only); smoother comment omits the 13th test in its no-op list.
Task 3: fix round 1/5 — implementer DONE b33d032 (581/581, +4; flag form for I-1 (marks kept); M-2 extended to NaN-poisoned smoother windows via !(prom>=k); redundant guard pairs documented). Re-review (sonnet) dispatched on review-119b033..b33d032.diff. NOTE for Task 6 brief: Input::ceilingDb default NaN ⇒ ceilingMissing; controller must set from preset.
Task 3: fix round 1/5 (6 addressed, 1 open — I-3 depthFor() direct call with NaN ceiling returns the raw rung (std::max(rung,NaN)=rung); also ceilingMissing not set on the nullptr early-return; commits 119b033..b33d032). Fix round 2/5 dispatched (resume implementer, one guard + one test).
Task 3: fix round 2/5 — implementer DONE 23c99f1 (582/582, +1 DepthForRefusesANonFiniteCeiling; flags computed above early return; both mutations isolate). Re-review (sonnet) dispatched on review-b33d032..23c99f1.diff
Task 3: fix round 2/5 (2 addressed, 0 open; commits b33d032..23c99f1)
Task 3: complete (commits 327762b..23c99f1, review clean after 2 fix rounds)
Task 3: NOTE for Task 6 brief — SoundcheckCandidates::Input::ceilingDb defaults NaN (=unset, refuse); controller MUST write it from the running preset; Output::ceilingMissing must be surfaced, never shown as "room clean"; pass Result::trusted verbatim.
Task 4: dispatched implementer (opus) at BASE 23c99f1, brief task-4-brief.md
Task 4: implementer DONE feebf82 (585/585, +3; 8 files, 3 functional src lines; 4 mutations). Reviewer (sonnet — small additive diff) dispatched on review-23c99f1..feebf82.diff
Branch: merged origin/fix/ci-macos-sessionlogger (PR #2, 4 lines) into lane M so CI has a green macOS base — 40818df
Task 4: review (sonnet) spec Compliant, quality Clean, 0 findings (no second SnapshotNotch initialiser; enum appended last; logstats keyed on reason inside notch_clear branch).
Task 4: minor (deferred): reasonName has no exhaustiveness guard (`return "unknown"` silent for a future enumerator).
Task 4: complete (commits 23c99f1..feebf82, review clean)
Task 5: dispatched implementer (opus) at BASE 40818df, brief task-5-brief.md — AUDIO CALLBACK task
CI NOTE 2026-09-16 02:00: GitHub Actions Windows job on 40818df not started — annotation: 'recent account payments have failed or your spending limit needs to be increased' (private repo minutes exhausted; macOS runner bills 10×). Owner action. Local ctest is the gate until fixed.
Task 5: implementer DONE 47d324c (595/595, +10, 0 deletions; 3 mutations red; two briefed tests strengthened: clamp test started inside ramp-in, snapshot-once test needed 20000 callbacks). Reviewer (opus — callback safety) dispatched on review-40818df..47d324c.diff. DO NOT RELEASE from this state (nothing drives the engine until Task 6).
Task 5: review (opus) spec conditional pass; inv-16 audit CONFIRMED (9 loads :629-638, 3 stores in point 3, 1 ring write, 1 RMW). 3 Critical lifecycle defects: (C1) soundcheck atomics survive device stop/restart — stale scOutChannel_ emits sweep on the NEXT device's channel N (audioDeviceAboutToStart :905-965 clears rings but not the atomics); (C2) scRampOutAtSample_ has no clearer except the callback's self-clear inside `if (scOut >= 0)` — device_changed/engine_stopped aborts hang the controller's "wait for -1" and a stale anchor truncates the NEXT run's sweep mid-way; (C3) mute-by-`continue` freezes the lane's NotchChain DF1 state for up to 4.5 s ⇒ un-mute free-response transient up to ~5-6× the level at mute onset (click at notch freq per channel). Important: (I1) TapsStaySuspendedAcrossTheGap's in-run half masked by the mute (lane routed to the measured output); (I2) ramp-out anchor snapshotted on the message thread ⇒ at buffers ≥1440 the first ramped block starts at envelope 0 = hard cut on the emergency-stop path. Minors M1–M6.
Task 5: CONTROLLER RULINGS — C1: clear the four soundcheck atomics beside micCapture_.clear() in audioDeviceAboutToStart. C2+I2 (one mechanism): add std::atomic<bool> scRampOutRequested_ set by requestSoundcheckRampOut(); the CALLBACK latches scRampOutAtSample_ = scSampleIndex on the first block that sees it (envelope starts at exactly 1.0 at any buffer size); setSoundcheckOutputChannel() clears scRampOutAtSample_ and scRampOutRequested_; controller abort backstop = setSoundcheckOutputChannel(-1). This makes EIGHT soundcheck atomics / ten loads — spec inv 7/16 text ("bảy") must be updated in Task 11 (docs), recorded here. C3 (DSP change, level statement required per CLAUDE.md): while a lane is muted, call chain.reset() for that lane every block (allocation-free, same call the NaN heal uses at :771) so the un-mute resumes from zero state; EXPECTED LEVEL CHANGE: mute onset = declared silence (spec §3); un-mute = programme resumes from silence with the notch chains at zero state — no free response from stale state; an in-flight lane-G depth ramp on a muted lane is stranded and self-heals on the next Set (lane G memory item, same as NaN heal). Owner listens in alpha. Fix round 1/5 (resume implementer opus).
Task 5: fix round 1/5 — implementer DONE 28dbd19 (600/600, +5; 9 mutants killed; eight atomics/ten loads/five stores; setSoundcheckOutputChannel now discards a pending ramp-out — Task 6 must not call it mid-ramp). Re-review (opus — callback) dispatched on review-47d324c..28dbd19.diff
Task 5: fix round 1/5 (9 addressed, 0 open; 2 NEW Important in the fix diff: (N1) muted-lane chain.reset() zeroes rampRemaining_ too ⇒ strands any in-flight lane-G depth ramp on every block for 4.5 s (model says target depth, filter at old depth) — fix: state-only Biquad::clearState()/NotchChain::clearState(); (N2) setSoundcheckOutputChannel's 'cleared BEFORE published' comment rests on relaxed stores on distinct atomics — publish release / load acquire; commits 47d324c..28dbd19). Fix round 2/5 dispatched. NOTE Task 6 brief: abort during NoiseFloor honoured only when index reaches 0 — keep setSoundcheckOutputChannel(-1) as backstop; audioDeviceAboutToStart now writes scSuspendTaps_/scCaptureActive_ — requires the abort+join-before-restart contract.
Task 5: fix round 2/5 — implementer DONE e0b8dc9 (603/603, +3: MuteDoesNotCancelAnInFlightDepthRamp, AbortDuringTheNoiseFloorIsDeferredUntilTheSweepStarts, SettingTheChannelToMinusOneIsTheAbortBackstop; Biquad/NotchChain::clearState() state-only; release/acquire on scOutChannel_ (not mutation-testable on x86)). Re-review (sonnet) dispatched on review-28dbd19..e0b8dc9.diff
Task 5: parked — NoiseFloor abort deferral: anchor latched while scSampleIndex_<0 is negative = the "-1 no-anchor" sentinel, so the ramp-out request is honoured only when the index reaches 0 — ruling: NO emission occurs during that window (amplitude already 0, inv 9 satisfied), worst case the channel stays muted/taps suspended ≤ ~0.53 s longer; Task 6 MUST call setSoundcheckOutputChannel(-1) as the abort backstop (test pins it). A sentinel change (INT64_MIN / validity bit) is deferred to final review, not smuggled into a round.
Task 5: fix round 2/5 (2 addressed, 0 open; commits 28dbd19..e0b8dc9)
Task 5: minor (deferred): M-4 unbraced else before nested for (:875); M-6 setSoundcheckGainUnclampedForTest unvalidated seam ships in release (Task 6 must never call it); getFilterForTest hands out a const Biquad& on a DSP class; callback self-clear store stays relaxed (fine).
Task 5: complete (commits 40818df..e0b8dc9, review clean after 2 fix rounds, 1 parked)
Task 6: dispatched implementer (opus) at BASE e0b8dc9, brief task-6-brief.md — SoundcheckController state machine
Task 6: implementer DONE_WITH_CONCERNS 93a7253 (627/627, +24; kNoiseFloorGuardMs 20 ms added — first build emitted 1.9 mV of sweep before the ringing gate aborted; per-channel 4.52 s ⇒ docs pass; inv 15 (live notches) deferred to Task 7; abortAndJoin() + real thread untested ⇒ Task 10; arm() refuses non-finite ceiling/zero gate/zero rate; OutputResult gained ceilingMissing/ladderMissing; commit trailer says Opus 5 not Fable). Reviewer (opus) dispatched on review-e0b8dc9..93a7253.diff
Task 6: review (opus) spec CONFORMS WITH 2 GAPS (S1 no ring-risk re-check at Arm — plan-mandated signature; S2 no backstop on the emitting abort path). Quality NOT YET TRUSTWORTHY: 3 Critical — (C1) noise-floor gate is a timing race: scOutChannel_ armed at enterTarget with index −520 ms, so a poll >20 ms late emits sweep into a ringing room; (C2) abortAndJoin() runs runOnce() on the message thread BEFORE joining the poll thread (plan-mandated order) ⇒ two threads in the machine, lane M can re-arm a full sweep after the abort; (C3) destructor/stop() mid-run leaves the engine armed+emitting, taps suspended, detection off, nobody left to restore. Important: I1 DetectionIsRestoredBeforeResults can't fail for the ordering; I2 gate max over all 1025 bins (0 Hz–Nyquist) — a 15 kHz LED whine or mains hum aborts the run; I3 abort with dead callback (engine_stopped) leaves channel armed; I4 arm() re-validates no target; I5 requestStop lifts nothing else, unbounded if the thread is dead. Minors: gate fails open on no data; arm() returns bare bool; NoiseFloorGateIsStableWithinARun can't fail; re-arm in the ramp-out window cuts the fade; progress getters untested.
Task 6: CONTROLLER RULINGS — C1: structural fix, do NOT arm scOutChannel_ during NoiseFloor; publish index + channel only after the gate returns quiet (kNoiseFloorGuardMs becomes a ramp lead-in). C2 (plan order was wrong): join FIRST, then runOnce() on the message thread, assert Idle; header contract: Task 10 must start() again after restart. C3: destructor and stop() stand the run down (backstop −1, capture off, taps lifted, detection on) — the class is the only writer of the atomics so it must restore them. S1 (plan-mandated signature): arm() takes the SnapshotBuffer and refuses RingRiskRising (same identity as preflight). S2/I3: backstop −1 unconditionally when !engine.isRunning(). I1, I2 (band [kSweepLowHz, kTrustedHighHz] like the detector), I4, I5 (requestStop also lifts scSuspendTaps_ — never the unsafe direction — plus the detection assertion) all fix. Minors: gate fails CLOSED (frames==0 ⇒ abort room_unknown? no — refuse with a distinct reason `noise_floor_unmeasured`), arm() returns Refusal, drop the duplicate stable-within-run test, arm() refuses while a ramp-out is pending (add AudioEngine accessor if needed), progress getters get one test. Fix round 1/5 (resume implementer opus).
Task 6: fix round 1/5 — implementer DONE 724b8f8 (637/637, +10 −1; 11 mutations red; output channel armed only after the gate; abortAndJoin join-first; stop/dtor stand down; arm(…, snapshot) refuses RingRiskRising; isSoundcheckRampOutPending() additive on AudioEngine). Re-review (opus) dispatched on review-93a7253..724b8f8.diff. NOTES: Task 7 must enforce inv 15 (liveNotchHz dedup); Task 10: lastDeviceError_ only cleared by start() ⇒ arm aborts instantly after a device error; Task 11 docs: 4.52 s/channel, ~72.3 s/16, gate ruler = mean power band-limited.
Task 6: fix round 1/5 (8 addressed, 2 PARTIAL — C-3 header note missing; S-2 expectEngineStoodDown absent from 3 abort tests; 2 NEW Important: (N1) after C-1 the Sweep/Tail deadlines still accumulate from the noise-floor deadline while the audio is anchored to the gate poll ⇒ poll lateness L shortens the tail by L and at L>700 ms enterGap() releases a NON-silent channel = hard cut; (N2) stop()/dtor now invoke the injected lambdas on the caller thread — header thread map + I-10 contract say otherwise ⇒ ~MainComponent trap; commits 93a7253..724b8f8). Fix round 2/5 dispatched.
Task 6: fix round 2/5 — implementer DONE 3c9563a (638/638; deadline re-stamped from the gate poll; releaseOutputChannelSafely() shared by enterGap/finishRun; header contracts for caller-thread lambdas + declaration order; expectEngineStoodDown in every abort test). Re-review (sonnet, scoped) dispatched on review-724b8f8..3c9563a.diff
Task 6: fix round 2/5 (4 addressed, 0 open; commits 724b8f8..3c9563a)
Task 6: minor (deferred): releaseOutputChannelSafely() lacks the `!engine_.isRunning()` backstop guard stopEmissionSafely() has (unreachable today; mirror it in Task 10 or final wave); arm() check-order untested for the compound case; DeviceError latches until start() ⇒ Task 10 surfaces it; docs (Task 11): 4.52 s/channel, ~72.3 s/16, gate = mean-power band-limited ruler; GUI-owned states Preflight/Confirm/Arm never entered by the machine (Task 9 must not drive the dialog from getState()).
Task 6: complete (commits e0b8dc9..3c9563a, review clean after 2 fix rounds)
Task 7: dispatched implementer (opus) at BASE 3c9563a, brief task-7-brief.md — APPLY/BỎ on the message thread; MUST enforce inv 15 (liveNotchHz dedup) since Task 6 passes it empty
Task 7: implementer DONE 96c2827+b7965e0 (652/652, +14; 6 mutations red; ApplyRunsOnTheMessageThread rewritten with a runtime MessageManager guard; inv 15 enforced here (±1 bin, excluding notches cleared this call); skippedLive ⊂ refused; SnapshotBuffer::linked ~10 ms stale ⇒ Task 10 must LOCK the LINK control while state != Idle incl. Results — ruling, not a new API). Reviewer (opus) dispatched on review-3c9563a..b7965e0.diff
Task 7: review (opus) — index allocation / linked unwind / replace ordering / thread guard all correct; setNotch(Origin::Soundcheck) does NOT override depth (KD-7 lives in placeConfirmed :1170-1171 only) so the ladder depth lands unchanged ✅. 2 Critical: (C1) inv 15 not applied to notches placed THIS call (no placedThisCall list; pick() has no mutual separation) ⇒ two adjacent-bin proposals cascade up to ~−48 dB where the ceiling allows −24; (C2) replace pass clears EVERY Origin::Soundcheck notch — but the legacy 15 s Soundcheck MODE (placeConfirmed :1093) is a second producer of that origin ⇒ an ÁP DỤNG silently deletes protection the operator locked in. Important: I1 result.slot never compared (copyResults() cross-places slots); I2 stale linked closable with EXISTING public effectiveLinked() (NotchController.h:223) read once at entry; I3 re-read half of B-1 has no failing test — relabel. Minors M1 placed/refused units; M2 takenThisCall keyed by index only (plan-mandated); M3 clearedPrevious counts snapshot entries; M4 routingInvalid untested; M5 depth frozen at Arm — slider lowered during the run/results ⇒ APPLY places deeper than the slider (lane G M-B lesson).
Task 7: CONTROLLER RULINGS — C1 fix (placedThisCall list). C2: NO new Origin enumerator (Q7 forbids new controller API; lane G's `origin != Soundcheck` checks would need auditing): scope the replace to the per-slot ledger of (lane, index, hz) recorded by the PREVIOUS apply, matched on active && origin==Soundcheck && |hz| within ±1 bin; legacy-mode notches untouched; ledger owned by the caller (`SoundcheckApplyLedger` in/out param, one per slot, kept by MainComponent in Task 10). Coordinator decision, to be recorded in the decision file (Task 11) as Q18 pending owner. I1 add `int slot` param + skip-and-count. I2 effectiveLinked() once at entry. I3 relabel comment+report. M2 per-lane bitmap. M5 self-clamp `std::max(cand.depthDb, controller.getNotchDepthDb())` at both setNotch sites (last gate before the PA). M1/M3 comments; M4 test. Fix round 1/5 (resume implementer opus).
Task 7: fix round 1/5 — implementer DONE ead1760 (658/658, +6; 12 mutations red; new signature applySoundcheckResults(controller, slot, results, ledger&); linked unwind strikes placedThisCall; NOTE Task 10: ledgers live for MainComponent's lifetime, one per slot). Re-review (opus) dispatched on review-b7965e0..ead1760.diff
Task 7: fix round 1/5 (10 addressed, 0 open; 2 NEW Important: (N1) ledger match breaks on sampleRate==0 / unmatched entry ⇒ old preventive notches orphaned (never auto-release) while the ledger is overwritten — the §4.6b chain-drain C-2 exists to prevent; (N2) unwind strike :1459 uncovered by any test; commits b7965e0..ead1760). Fix round 2/5 dispatched. NOTES Task 9/10: CLEAR ALL does not reset SoundcheckApplyLedger (harmless, state it); clearedPrevious>0 && placed==0 on a full chain must not read as success; result.lane bounds check.
Task 7: fix round 2/5 — implementer DONE 5153694 (662/662, +4; 15 mutations red; rateKnown change unreachable today (Detector rejects rate<=0) — documented, no test; re-run inside one hop overwrites index 15 with no SoundcheckReplace log line — lane D label caveat). Re-review (sonnet, scoped) dispatched on review-ead1760..5153694.diff
Task 7: fix round 2/5 (5 addressed, 0 open; commits ead1760..5153694)
Task 7: minor (deferred): a ledger entry orphaned by CLEAR ALL/width change is carried forward indefinitely (bounded by kTotalSlots) and could attribute a future Soundcheck-origin notch at the same (lane,index) within ±1 bin to this ledger — documented in the header, low probability (Task 10 could reset the ledger on CLEAR ALL); a re-run inside one hop overwrites index 15 with no SoundcheckReplace log line (lane D label caveat); setNotchImpl accepts depth==0 (pre-existing, NotchController-owned).
Task 7: complete (commits 3c9563a..5153694, review clean after 2 fix rounds)
Task 8: dispatched implementer (opus) at BASE 5153694, brief task-8-brief.md — five soundcheck_* JSONL events
Task 8: implementer DONE 8e85484 (668/668, +6; 3 mutations red; makeSoundcheckApplyEvent added (no producer existed); fixture 7 lines; RULING pending review: soundcheck_apply must also carry skippedOtherSlot/skippedBadLane (fault signals) — fold into fix round or Task 10). Reviewer (sonnet) dispatched on review-5153694..8e85484.diff
Task 8: review (sonnet) spec compliant, quality sound; 1 Important: soundcheck_apply drops skippedOtherSlot/skippedBadLane (distinct faults) — ruling: add both (not skippedLive, a refused subset); minor: at_output = current in-progress output (document); start event's extra 'gate' field undisclosed in report. Fix round 1/5 (resume implementer).
Task 8: fix round 1/5 — implementer DONE f8437df (668/668; apply event carries skipped_other_slot/skipped_bad_lane, skipped_live asserted absent; at_output comment). Re-review (sonnet) dispatched on review-8e85484..f8437df.diff
Task 8: fix round 1/5 (3 addressed, 0 open; commits 8e85484..f8437df)
Task 8: minor (deferred): no real SessionLogger file round-trip test for the five events (Task 10/11 could add one); makeSoundcheckApplyEvent has no production caller until Task 10 (must use it, not inline a second shape).
Task 8: complete (commits 5153694..f8437df, review clean after 1 fix round)
Task 9: dispatched implementer (opus) at BASE f8437df, brief task-9-brief.md — GUI (ĐO button, progress overlay + DỪNG, results strip ÁP DỤNG/BỎ, locked controls, SpectrumView margin overlay, snapshot scene) — MUST end with rendered PNGs read back
Task 9: implementer DONE f7a2272 (683/683, +15; render found 3 defects the tests passed — strip over analyser, invisible low-confidence wash, curve in the peak-hold colour — fixed; live/idle/preset PNGs pixel-diffed: only the ĐO cell bbox changed; coordinator READ both new PNGs: legible, Vietnamese intact, strip below plot). Reviewer (opus) dispatched on review-f8437df..f7a2272.diff. NOTE: implementer claims memory juce9-api-traps two-arg TextButton is wrong for this JUCE build — verify in final review before editing memory.
Task 9: review (opus) spec substantially compliant; 0 Critical; 4 Important: (I1) paint allocates every frame (String::fromUTF8 + Font per paint) — plan-mandated no-alloc; (I2) low-confidence veil ~3% luminance, invisible in the PNG; (I3) three source comments claim the 2-arg TextButton ctor renders NO text — JUCE source (juce_TextButton.cpp:46-49) says param 2 is the tooltip; memory note's RULE is right, only its generalisation is wrong ⇒ fix the comments, leave the memory; (I4) short-window fallback overlays the plot AND silently drops fault sentences. Minors M1 ĐO face not accented (buttonOnColourId only lights toggled buttons); M2 margin curve white == peak-hold white; M3 snapshot fixture dips narrower than bin grid (247 Hz tick has no peak); M4 hotSpots==0 && saturated>0 contradiction. Missing: F10 lock has no caller (Task 10), no test pins the layout fix, no hover readout. Judgement: add a landing place for clearedPrevious>0 && placed==0 now (Model::worseOff + danger sentence). Fix round 1/5 (resume implementer opus).
Task 9: fix round 1/5 — implementer DONE 7ef8848 (689/689, +6; veil sampled (10,11,13)→(29,31,36); ĐO legend = accent exactly; own M-2 dash fix initially erased the low half of the curve — caught by render, fixed, pinned by bbox test; Mode::Applied + worseOff exist for Task 10). Re-review (sonnet) dispatched on review-f7a2272..7ef8848.diff
Task 9: fix round 1/5 (10 addressed, 0 open; 1 NEW Important (a): kSoundcheckOverlayReserveFloats=5000 derived from a 2560 px max the app does not enforce (setResizeLimits 16384) — a 5120 px ultrawide exceeds the reserve ⇒ Path reallocates in paint; commits f7a2272..7ef8848). Fix round 2/5 (one item).
Task 9: fix round 2/5 — implementer DONE c6dd05c (689/689; kMarginDashPeriods=110 width-independent, reserve 4200; 605 elements at 900 and 8192 px; mutation 1718@8192 fails). Re-review (sonnet) dispatched on review-7ef8848..c6dd05c.diff
Task 9: fix round 2/5 (1 addressed, 0 open; commits 7ef8848..c6dd05c)
Task 9: minor (deferred): ĐO hint Vietnamese while the three mode hints are English (deliberate, mixed); very short console can drive the analyser to 0 px (chosen direction); element count is a proxy for "did not reallocate" (Path has no capacity getter); header break-case comment terser than the proof.
Task 9: complete (commits f8437df..c6dd05c, review clean after 2 fix rounds)
Task 10: dispatched implementer (opus) at BASE c6dd05c, brief task-10-brief.md — MainComponent wiring, DevicePanel hooks, locks, ledgers, Mode::Applied
Task 10: implementer DONE fcecfe5 (702/702, +13; 4 mutations red; soundcheck_ declared LAST; unlocks at Mode::Applied; Esc unreachable from GUI (panel routes both via onStop); APPLY end-to-end not reachable headless ⇒ first real ÁP DỤNG on a rig at low volume; targets not deduped by output channel). Reviewer (opus) dispatched on review-c6dd05c..fcecfe5.diff
Task 10: review (opus) spec substantially compliant; declaration order + single arm() call site + D8 praised. 1 Critical: (C1) notchListPanel_ (sibling child) not in the lock set — FALSE verdict button clears a live notch mid-sweep. Important: I1 tuningPanel_ not locked (DEPTH/Q move during run ⇒ APPLY writes against a stale ceiling); I2 ĐO not disabled while its own dialog is open (stacked dialogs, stale OK arms a later run); I3 Results window locks BYPASS/CLEAR ALL for 20 s with detection ON — spec §4.3 says unlock everything except PRESET LOAD; ModeRail already has the split setters. Minors: M1 confirm text says "kênh ngõ ra" for target count; M2 restart during Applied claims a measurement stopped; M3 ≤200 ms detection outside mode rules at session end (comment); M4 abortAndJoin()==false not acted on (jassertfalse + distinct sentence). Also: soundcheck_.start() in startAudio() uncovered by a test; Esc unreachable from GUI (Task 9 surface, follow-up list); soundcheck_apply logged per slot with a `slot` field (Task 11 note for log readers).
Task 10: CONTROLLER RULINGS — C1, I1, I2 fix. I3: follow spec §4.3 with the split setters: while state==Results (and Applied) rail mode switches + CLEAR ALL enabled, ĐO disabled, PRESET LOAD refused; slotPanel_ (LINK/width — Task 7's stale-linked ruling), tuningPanel_ (frozen RunParams) and notchListPanel_ stay LOCKED until dismiss/apply-complete; full lock while measuring. M1–M4 fix. Fix round 1/5 (resume implementer opus).
Task 10: fix round 1/5 — implementer DONE 2a7bdae (707/707, +5; SoundcheckLock{None,Measuring,Pending}; 6 mutations red; ĐO dimness false alarm (measured 255,159,28); startAudio start() untested by choice (opens a real device = feedback path in the suite)). Re-review (opus) dispatched on review-fcecfe5..2a7bdae.diff
Task 10: fix round 1/5 (6 addressed, 2 PARTIAL, 1 NOT; commits fcecfe5..2a7bdae). NEW Critical: (C1) setSoundcheckLock re-enables ĐO ignoring soundcheckConfirmPending_, and the stale-OK guard (state!=Idle || lock!=None) misses a device restart while idle ⇒ a stale OK arms a sweep on a different rig. Important: (I2) M-3's "≤200 ms" bound is false — after a successful run detection is live for the whole Results window (≤20 s) with no applyModeGating, incl. in BYPASS (KD-9); disabled slots never re-gated. (I3) nothing proves the app ever enters Pending — every lock test uses the test setter; inverting the Applied→Pending ternary leaves 707 green. Out-of-scope but high value: poll-thread aborts (MicHot, DeviceChanged, RoomRinging, …) vanish the strip with NO sentence on screen. Ruling: Applied→Pending with no timeout stays (BỎ is one click) — tester notes must say it. Fix round 2/5 dispatched.
Task 10: fix round 2/5 — implementer DONE 17f5225 (712/712, +5; 6 mutations red; generation counter; updateMeasureEnabled single writer; gate at Results edge + disabled slots gated; public getLastAbortReason(); nine abort sentences; applyModeGating behaviour change outside lane M (disabled slot detector now turned OFF) — owner's eye). Re-review (sonnet, scoped) dispatched on review-2a7bdae..17f5225.diff
Task 10: fix round 2/5 (4 addressed, 0 open; commits 2a7bdae..17f5225)
Task 10: minor (deferred): Results-edge gating call (:1725) not directly test-covered (no ClockSource seam on MainComponent — same gap as I-3); loadPreset's slot ingestion does not call applyModeGating (pre-existing); APPLY path unreachable headless ⇒ FIRST REAL ÁP DỤNG IS ON A RIG AT LOW VOLUME (tester notes); Applied report holds routing/tuning/verdicts until BỎ (no timeout); getRemainingMsInRun() reports full length once Idle; startAudio()'s soundcheck_.start() untested by choice; targets not deduplicated by output channel (visible in dialog); Esc indistinguishable from DỪNG in the log.
Task 10: complete (commits c6dd05c..17f5225, review clean after 2 fix rounds)
Task 11: dispatched implementer (opus) at BASE 17f5225, brief task-11-brief.md + THIS LEDGER's NOTE/ruling lines as the source of truth for what changed vs spec rev 4
FINAL: whole-branch review (opus) dispatched on review-a6be099..17f5225.diff (code only; docs Task 11 running in parallel); independent verifier (az-harness:verifier) dispatched to rebuild + ctest from scratch and refute the ledger's claims
VERIFIER (independent, clean rebuild outside repo): 712/712 twice; racy tests 5/5 each; test count 711+1; snapshot tool never arms; 7 seams unreachable from src/tools; callback 10 loads in snapshot block, 24 atomic ops total, zero forbidden calls; fixture LF in index; 1 REFUTED claim: 'zero new warnings' — SoundcheckPanel.cpp:302 C4459 'text' hides global (lane M code, never seen because build/ was incremental) → fold into the final fix wave. Docs drift (bảy atomic, 4,5 s) confirmed pending Task 11.
FINAL REVIEW (opus, whole branch a6be099..17f5225): READY-FOR-OWNER; 0 Critical; inv 16 CONFIRMED by hand (10 loads at :664-680, 5 stores under scOut>=0, one ring write, one RMW; guard byte-identical to main, still after injection; idle path identical). 3 Important: (I1) confirm dialog omits "detection is OFF on every slot for the whole run — press DỪNG if the room starts to howl" (only in-run backstop on non-measured channels is the −6 dBFS mic RMS); (I2) ring-risk gate at preflight/arm reads only displayedSlot_ while arm() disarms every slot — take the worst over enabled slots (log the displayed one); (I3) worseOff summed across slots — a slot left unprotected is masked by another's success; per-slot verdict. Minors: M1 std::normal_distribution not portable (MSVC vs libc++) in two new test fixtures — Box-Muller from uniform; M2 micCapture_.clear() one-block lag comment; M3 isfinite guard on setSoundcheckGainUnclampedForTest; M4 laneCount fallback doc; M5 stale kNoiseFloorGuardMs in a test comment; M6 unbraced else; M7 fixture residual_db −1.8 impossible. Ledger triage: all deferred/parked OK-TO-SHIP except: first real ÁP DỤNG on rig, Applied holds console until BỎ, ĐO hint language, one-hop re-run log gap — OWNER-DECISION/tester notes; the NoiseFloor abort park is now largely unreachable (channel not armed during the floor) — do not spend a sentinel change on it. Rig checklist (10 items, ordered) recorded in the review for the tester notes.
FINAL FIX WAVE (one dispatch, after Task 11 docs lands to avoid concurrent commits): I1, I2, I3, M1, M3, M5, M6, M7, M2 comment + verifier's C4459 at SoundcheckPanel.cpp:302.
Task 11: complete (commits 989f9a0, a5fc85a, f9dbad3 — docs, spec rev 5 §9 26 rows, Q18–Q23, release note 1.3.0, tester notes, memory). FINAL FIX WAVE dispatched (opus) at BASE f9dbad3.
FINAL FIX WAVE: implementer DONE 0bb5bda+94489a6+ed4d74d (714/714 clean build; 0 warnings in lane-M files; 3 mutations red; worstRingRiskSnapshot() over enabled slots + displayed; ring_risk_slot field; kMaxSummaryLines 7; Box–Muller fixtures; SDD directory now TRACKED in ed4d74d). Scoped re-review (opus) dispatched.
FINAL FIX WAVE: re-review (opus) — all 11 items ADDRESSED; READY-FOR-OWNER: YES; 0 Critical; 2 non-blocking Important parked with rulings: (P1) the 7-line Applied+worseOff+every-fault summary is height-safe by construction (preferredHeight from summaryLineCount_) but no test pins the 7-line case — ruling: real, non-load-bearing, first follow-up after owner review; (P2) final-fix-wave-report.md lacks an M-7 entry — ruling: report gap only, code verified against SoundcheckCandidates.cpp:64. Observation parked: danger sentence prints the cross-slot TOTAL clearedPrevious (over-warns — safe direction) — follow-up. `ring_risk_slot` not pinned by a test / not in spec §4.7 table — follow-up docs.
BRANCH STATE 2026-09-16: feat/lane-m-active-soundcheck @ ed4d74d + this ledger commit; 714/714 clean build; 0 warnings in lane-M files; expected level change of the fix wave: none. RELEASE 1.3.0 (Task 12) BLOCKED on the owner: 15 Q items (Q2, Q15↺, Q6, Q3↺, Q16, Q7+Q9, kResultsTimeoutMs, ClearReason::SoundcheckReplace, SnapshotNotch::origin, Q18–Q23) + "merge" for PRs #2→#3→#1→#4 + GitHub Actions billing. Rig checklist (10 ordered items) in the final review text and installer/TESTER-NOTES.md.

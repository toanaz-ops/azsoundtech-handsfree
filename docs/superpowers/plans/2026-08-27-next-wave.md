# Next Wave — preset chain, ring-risk, threshold sweep, hạ tầng

> **For agentic workers:** REQUIRED SUB-SKILL: dùng
> superpowers:subagent-driven-development (khuyên dùng) hoặc
> superpowers:executing-plans để chạy từng task. Checkbox (`- [ ]`) để track.

**Goal:** Đóng nốt 3 mảng dở dang sau đợt review 27/08 (chuỗi preset, RING
RISK, ngưỡng peakiness với FFT 2048) và vá 3 việc hạ tầng nhỏ.

**Architecture:** 4 lane độc lập, mỗi lane = 1 session = 1 worktree = 1
branch. Lane P và R có một vùng chạm chung duy nhất (`MainComponent`) — xem
ma trận song song ở cuối.

**Tech Stack:** C++20 / JUCE 9 / CMake (VS 18 2026) / GoogleTest / NSIS /
PowerShell 7.

## Global Constraints

- Build: `cmake -B build -G "Visual Studio 18 2026" -A x64` rồi
  `cmake --build build --config Release`; test `cd build && ctest -C Release`.
  Baseline hiện tại: **366/366 pass** (v1.0.4, commit `5c45fcc`).
- Đường audio real-time: không cấp phát, không mutex, không log — mọi guard
  hiện có (FTZ/DAZ, Nyquist, width validation, clamp ±1.0 + NaN sanitize)
  KHÔNG được gỡ (CLAUDE.md, global rule 10).
- Thay đổi hằng số DSP/topology/hành vi user-visible ⇒ cập nhật
  `docs/GIOI-THIEU.md` + `docs/KY-THUAT-CHONG-HU.md` cùng change
  (Definition of done #5).
- Thay đổi GUI ⇒ chụp `build/tools/Release/HandsFreeSnapshot.exe shots 1440
  920 --fast`, ĐỌC lại ảnh trước khi báo cáo.
- Dialog/FileChooser async phải injectable + `Component::SafePointer`
  (bài học `memory/gui-console-lessons-2026-08-24.md`).
- JUCE 9: không dùng brace-init 2 tham số cho widget —
  `TextButton{name, x}` coi x là tooltip (memory `juce9-api-traps`).
- Xong lane ⇒ release alpha: `pwsh -File installer/release-alpha.ps1`
  (gate ctest; listen-before-release đã bỏ 27/08 — nghe trong alpha).
  Worktree mới cần copy `external/asiosdk/` và `installer/vendor/` từ
  checkout chính (cả hai untracked).

---

# LANE P — Chuỗi preset trọn vẹn (session P)

Ba mảnh đã viết-và-test nhưng chưa nối: `MainComponent::loadPreset()` không
có caller; `presetfirstrun::seedDefaultPresets()` chỉ test gọi; installer
không chép `presets/`. Kết quả: user cài app KHÔNG có Speech/Music và không
có cách nạp/lưu preset.

### Task P1: Installer ship `presets/` vào `$INSTDIR\presets`

**Files:**
- Modify: `installer/handsfree.nsi` (~dòng 149–150, sau `File "${APP_EXE_SRC}"`)
- Modify: `installer/handsfree.nsi` — section Uninstall (xóa `$INSTDIR\presets`)

**Interfaces:**
- Produces: thư mục `$INSTDIR\presets\Speech.json`, `Music.json` cho Task P2
  đọc lúc first-run.

- [x] **Step 1: thêm File lines** — sau `SetOutPath "$INSTDIR"` /
  `File "${APP_EXE_SRC}"`:

```nsis
  ; Shipped default presets. The app seeds these into %APPDATA% on first
  ; run (PresetFirstRun) and NEVER overwrites a file the user already has.
  SetOutPath "$INSTDIR\presets"
  File "${PRESETS_SRC}\Speech.json"
  File "${PRESETS_SRC}\Music.json"
  SetOutPath "$INSTDIR"
```

  Định nghĩa `PRESETS_SRC` cạnh `APP_EXE_SRC` ở đầu file (trỏ
  `..\presets`), theo đúng kiểu path-relative-to-nsi mà file này đã ghi chú
  kỹ ở dòng ~50–57.
- [x] **Step 2: uninstaller** — thêm `Delete "$INSTDIR\presets\*.json"` +
  `RMDir "$INSTDIR\presets"` vào section Uninstall (phần xóa `$INSTDIR`,
  KHÔNG đụng khối hỏi xóa `%APPDATA%` ở ~dòng 281–293).
- [x] **Step 3: verify bằng makensis** — build installer local:
  `pwsh -File installer/release-alpha.ps1 -SkipTests` (giờ chỉ build vào
  `installer\dist-local\`, không publish). Mở exe bằng 7z/NSIS list hoặc cài
  vào máy ảo/thư mục tạm, xác nhận `presets\Speech.json` có mặt.
- [x] **Step 4: commit** — `git add installer/handsfree.nsi` +
  `git commit -m "build(installer): ship default presets into INSTDIR/presets"`.

### Task P2: App gọi seed lúc khởi động

**Files:**
- Modify: `src/app/MainComponent.cpp` (constructor) hoặc `src/main.cpp` —
  đọc cả hai, đặt seed ở nơi chạy TRƯỚC khi DeviceDrawer/PresetManager cần
  tới thư mục user; MainComponent ctor là ứng viên chính.
- Test: `tests/test_presetsfirstrun.cpp` (đã có 4 test hàm seed — thêm test
  wiring nếu tách được, không thì test thủ công theo Step 3).

**Interfaces:**
- Consumes: `presetfirstrun::seedDefaultPresets(const juce::File& sourceDir,
  const juce::File& targetDir)` → `SeedResult{copied, skipped, errors}`
  (src/app/PresetFirstRun.h:43).
- Consumes: thư mục user chuẩn `%APPDATA%/AZSoundtech/HandsFree/presets`
  (PresetManager.h:211 đã document — dùng helper sẵn có ở đó nếu có, grep
  `defaultPresetDirectory` trước khi tự dựng path).

- [x] **Step 1: viết wiring** — trong MainComponent ctor (ngoài audio path,
  message thread, chạy một lần):

```cpp
    // Seed the shipped presets exe-adjacent -> user dir, never overwriting.
    // Source: <exe dir>/presets (the installer puts them there, P1); running
    // from the repo the folder is presets/ next to the build tree -- when the
    // source dir is missing SeedResult just reports it and the app carries on.
    {
        const auto exeDir = juce::File::getSpecialLocation (
            juce::File::currentExecutableFile).getParentDirectory();
        const auto seeded = presetfirstrun::seedDefaultPresets (
            exeDir.getChildFile ("presets"),
            PresetManager::getPresetDirectory());  // PresetManager.h:215
        // Log ngoài audio thread là hợp lệ ở đây nếu app đã có pattern log.
    }
```

  `PresetManager::getPresetDirectory()` là helper chuẩn
  (`%APPDATA%/AZSoundtech/HandsFree/presets`, hỏi JUCE chứ không hardcode) —
  không tự dựng path.
- [x] **Step 2: build + ctest** — 4 test PresetFirstRun hiện có phải còn
  xanh (chúng pin hành vi never-overwrite).
- [x] **Step 3: chạy tay** — chạy `build/HandsFree...exe` (hoặc app target
  thật) một lần, xác nhận `%APPDATA%\AZSoundtech\HandsFree\presets\` xuất
  hiện Speech.json + Music.json; chạy lần 2, sửa nội dung 1 file, chạy lần
  3 → file KHÔNG bị ghi đè.
- [x] **Step 4: commit.**

### Task P3: GUI nạp/lưu preset (LOAD… / SAVE…)

**Files:**
- Modify: `src/gui/DeviceDrawer.h/.cpp` — thêm hàng PRESET (2 nút) dưới
  section INTERFACE. DeviceDrawer.h:7-10 ghi placeholder "(presets)" từng bị
  gỡ có chủ đích — đọc note đó trước, đặt lại đúng chỗ nó mô tả.
- Modify: `src/app/MainComponent.h/.cpp` — nối callback 2 nút vào
  `loadPreset(file)` (đã có, MainComponent.h:88) và hàm mới
  `savePreset(file)` gọi `PresetManager::saveToFile` (PresetManager.h:252).
- Test: `tests/test_gui_wiring.cpp` + `tests/test_presetmanager.cpp`.

**Interfaces:**
- Consumes: `bool MainComponent::loadPreset (const juce::File&)`;
  `static bool PresetManager::saveToFile (const Preset&, ...)` — đọc chữ ký
  đầy đủ tại PresetManager.h:241-252 trước khi viết.
- Produces: `bool MainComponent::savePreset (const juce::File&)` — gom trạng
  thái notch/tuning hiện tại thành `Preset` rồi saveToFile.

- [x] **Step 1: failing test wiring** — theo pattern test confirm-dialog
  injectable của ModeRail (xem `tests/test_moderail.cpp`): FileChooser phải
  đứng sau một `std::function` injectable để test thay bằng fake, KHÔNG mở
  dialog thật trong test. Ví dụ khung:

```cpp
TEST (GuiWiring, LoadPresetButtonRoutesThroughInjectableChooserToLoadPreset)
{
    // fake chooser trả về một file JSON hợp lệ đã ghi sẵn vào temp dir
    // expect: loadPreset được gọi, bảng notch phản ánh nội dung file
}
TEST (GuiWiring, SavePresetWritesAFileThatLoadPresetReopensIdentically)
{
    // round-trip qua temp dir; so nội dung Preset, không so text từng byte
}
```

- [x] **Step 2: chạy RED.**
- [x] **Step 3: implement** — nút bằng
  `std::make_unique<juce::TextButton>("LOAD...")` (KHÔNG brace-init 2 tham
  số — JUCE 9 trap); `juce::FileChooser::launchAsync` với
  `Component::SafePointer` trong lambda; filter `*.json`; thư mục mở mặc
  định = thư mục user preset (cùng helper Task P2).
- [x] **Step 4: GREEN + full ctest.**
- [x] **Step 5: snapshot** — chạy HandsFreeSnapshot, ĐỌC ảnh: 2 nút có chữ,
  không cụt, không đè layout INTERFACE hiện có. Gửi ảnh cho chủ dự án.
- [x] **Step 6: cập nhật docs** — GIOI-THIEU.md (bỏ caveat "chưa có nút
  nạp/lưu", bỏ caveat seed) + KY-THUAT-CHONG-HU.md (§4 preset, §7 status)
  + `docs/superpowers/plans/2026-08-19-az-soundtech-hands-free.md` dòng
  Tasks 25/26.
- [x] **Step 7: commit + release alpha** (`release-alpha.ps1`, cập nhật
  TESTER-NOTES.md: bỏ mục "preset chưa tự cài").

---

# LANE R — RING RISK data provider (session R)

Toàn bộ contract đã có ở `docs/spec-ring-risk.md` — ĐỌC TRỌN file đó trước
khi viết dòng nào. Tóm tắt các mốc phải theo:

### Task R1: Publish score vào SnapshotBuffer

**Files:**
- Modify: `src/app/NotchController.h` (struct SnapshotBuffer),
  `src/app/NotchController.cpp` (điểm publish snapshot hiện có)
- Test: `tests/test_notchcontroller.cpp`

**Interfaces:**
- Produces (đúng theo spec-ring-risk.md §1):

```cpp
    float ringRiskScore = 0.0f;   // max candidate confidence của frame; 0 khi không bin nào scoreable
    bool  ringRiskValid = false;  // false tới khi detector đủ history
    float ringRiskThreshold = 0.0f; // threshold sống (spec §2: không hardcode 10.0 ở GUI)
```

- [ ] **Step 1: failing test** — publish một frame có candidate confidence
  X → snapshot đọc ra ringRiskScore == X, valid == true; trước frame đầu →
  valid == false, GUI-side phải hiểu là Unavailable.
- [ ] **Step 2: RED.**
- [ ] **Step 3: implement** — ghi cả 3 field trong CÙNG bước publish đang
  ghi `magnitudes`/`notchCount`, dưới cùng mutex snapshot. Không lock mới,
  không thread mới (spec §1). Giá trị lấy từ đúng con số
  `CandidateScorer::scoreCandidate` dùng để quyết định đặt notch — KHÔNG
  tính lại một peakiness thứ hai (spec: "What is missing").
- [ ] **Step 4: GREEN + full ctest.** Commit.

### Task R2: Map score → state trong GUI, có hysteresis

**Files:**
- Modify: `src/gui/SpectrumView.h/.cpp` — hàm
  `static RingRisk riskForScore (const NotchController::SnapshotBuffer&)`
  + hysteresis
- Test: `tests/test_spectrumview.cpp`

**Interfaces:**
- Consumes: 3 field Task R1; enum `RingRisk{Unavailable,Low,Rising,Critical}`
  (SpectrumView.h:143).
- Banding theo spec §2: `!valid→Unavailable`; `<0.55×thr→Low`;
  `<thr→Rising`; `≥thr→Critical`. Hysteresis theo spec §3 — chọn MỘT trong
  hai (hold 750 ms không step-down, hoặc fall tại 0.85×) và ghi chú trong
  code.

- [ ] **Step 1: failing tests** — 4 band + 1 test hysteresis (score dao
  động quanh biên không được flicker) + giữ nguyên test pin
  `RingRiskReadsUnavailableUntilSomethingProvidesIt` (KHÔNG được sửa nó —
  spec cấm default về Low).
- [ ] **Step 2: RED → implement → GREEN.** Commit.

### Task R3: Wiring 1 dòng + acceptance

**Files:**
- Modify: `src/app/MainComponent.cpp` (ctor, cạnh các provider lambda khác)
- Test: `tests/test_gui_wiring.cpp`

- [ ] **Step 1:** wiring đúng snippet spec §4 — đọc controller của slot
  ĐANG monitor (`displayedSlot_`), không phải slot 0.
- [ ] **Step 2:** full ctest + snapshot (chip phải hiện N/A khi idle — ảnh
  console-idle; console-live của snapshot tool là synthetic nên vẫn N/A trừ
  khi tool bơm snapshot — ghi rõ trong báo cáo).
- [ ] **Step 3:** acceptance 1–4 của spec §Acceptance chạy được đến đâu ghi
  đến đó; mục 2–3 (tone 1 kHz thật, noise thật) cần rig — chuyển sang
  SESSION T/human, ghi vào TESTER-NOTES thay vì tự nhận xong.
- [ ] **Step 4:** docs (GIOI-THIEU "Theo dõi khi chạy", KY-THUAT, và sửa
  `docs/spec-ring-risk.md` status header thành "implemented, chờ kiểm chứng
  rig"). Commit + release alpha, TESTER-NOTES bỏ mục "N/A không phải bug",
  thay bằng hướng dẫn đọc chip.

---

# LANE T — Sweep lại ngưỡng peakiness với FFT 2048 (session T + NGƯỜI)

Ngưỡng 10.0 và bảng đo trong `src/dsp/PeakinessAnalyzer.h:53-57,118-125` đo
trên hình học 1024. FFT 2048 làm bin hẹp một nửa → phân bố peakiness của
noise/nhạc/hú có thể dịch. Đây là lane DUY NHẤT cần phần cứng.

### Task T1 (agent làm trước, không cần rig): harness sweep offline

**Files:**
- Create: `tools/peakiness_sweep.cpp` (+ đăng ký trong `tools/CMakeLists.txt`
  theo đúng pattern HandsFreeSnapshot)
- Test: không cần test riêng — tool offline, output là số liệu.

**Interfaces:**
- Consumes: `PeakinessAnalyzer` + `Detector` public API (đọc
  `tests/test_peakiness.cpp` để lấy cách bơm tín hiệu tổng hợp sẵn có).

- [ ] **Step 1:** tool đọc WAV (juce::AudioFormatManager) hoặc sinh tín
  hiệu tổng hợp (sine ramp-up giả hú, pink noise, nhạc từ file), chạy qua
  Detector@2048, in CSV: `block, freq, peakiness, score, threshold`.
- [ ] **Step 2:** chạy trên bộ tín hiệu tổng hợp, in phân bố: noise floor
  peak ~bao nhiêu (baseline cũ: ~7.35 vs thr 10.0 — test_peakiness đã pin),
  hú giả cross bao nhiêu. Paste bảng vào báo cáo.
- [ ] **Step 3:** commit tool.

### Task T2 (NGƯỜI + rig, hẹn lịch với chủ dự án)

- [ ] Thu WAV thật tại rehearsal room (iD14/Wing): noise sàn, nhạc, hú thật
  gây có kiểm soát ở volume thấp.
- [ ] Chạy T1 trên các file thu; nếu phân bố lệch → đề xuất ngưỡng mới +
  cập nhật bảng đo trong PeakinessAnalyzer.h + test_peakiness pins; nếu
  không lệch → chỉ cập nhật comment "đã đo lại với 2048, giữ 10.0" và xóa
  caveat trong KY-THUAT-CHONG-HU.md §3.2.
- [ ] Nhân buổi này chạy luôn acceptance 2–3 của LANE R (tone 1 kHz →
  Critical trước khi notch hiện; noise → Low) và nghe clamp 1.0.4.

---

# LANE C — Hạ tầng nhỏ (session C)

### Task C1: Prune verify-after-delete (Google Drive xóa async)

**Files:** Modify `installer/release-alpha.ps1` (khối prune, ~dòng 234-259)

Sự cố 27/08: `Remove-Item` trên `Z:\` (Google Drive) báo thành công nhưng
`...-1.0.0.exe` vẫn nằm lại, phải xóa tay.

- [x] **Step 1:** sau mỗi Remove-Item trong prune, thêm:

```powershell
        Start-Sleep -Milliseconds 500
        if (Test-Path $stale.FullName) {
            Write-Host "WARN  $($stale.Name) still present after delete (Drive sync lag?) -- retrying once"
            Remove-Item $stale.FullName -Force
            Start-Sleep -Milliseconds 1500
            if (Test-Path $stale.FullName) {
                Write-Host "WARN  $($stale.Name) would not delete -- tidy the drop folder by hand"
            }
        }
```

- [x] **Step 2:** verify parse (`ParseFile` như lần trước) + mô phỏng bằng
  thư mục tạm local. KHÔNG chạy script thật.
- [x] **Step 3:** commit.

### Task C2: Quyết định 2 món mồ côi (CẦN CHỦ DỰ ÁN GẬT TRƯỚC KHI XÓA)

- [x] Hỏi chủ dự án: (a) xóa `skills-lock.json`? (b) xóa thư mục
  `openspec/`? — Resolved 2026-09-04 (commit 7c91ee6): chủ dự án gật xóa
  (a), NHƯNG khi điều tra (b) phát hiện `.opencode/skills/openspec-*`
  (OpenCode's `/opsx-*` commands) đang đọc `openspec/config.yaml` — CLAUDE.md
  từng ghi "not in use" là SAI (đúng cho Claude Code, sai cho OpenCode). Chủ
  dự án chốt: GIỮ `openspec/`. Xem `memory/docs-drift-audit-2026-08-27.md`.
- [x] `git rm skills-lock.json` + commit (giữ nguyên `openspec/config.yaml`
  — KHÔNG xóa, khác với đề xuất ban đầu của plan này). CLAUDE.md đã sửa
  lại claim "not in use" trong cùng commit.

### Task C3 (chờ người, không phải việc agent): mua chứng chỉ code signing

- [ ] Task 31 của roadmap — SmartScreen sẽ còn chặn tới khi có cert. Chỉ
  chủ dự án làm được (mua EV/OV cert). Khi có cert: thêm bước `signtool`
  vào release-alpha.ps1 (task mới lúc đó).

---

# MA TRẬN SONG SONG

| | Lane P (preset) | Lane R (ring-risk) | Lane T1 (sweep tool) | Lane C (hạ tầng) |
|---|---|---|---|---|
| **File đụng** | DeviceDrawer, MainComponent, handsfree.nsi, presets docs | NotchController, SpectrumView, MainComponent (1 dòng ctor) | tools/ mới | release-alpha.ps1, 2 file mồ côi |
| **Lane P** | — | ⚠ chạm chung `MainComponent.cpp` | ✅ | ✅ |
| **Lane R** | ⚠ | — | ✅ | ✅ |
| **Lane T1** | ✅ | ✅ | — | ✅ |
| **Lane C** | ✅ | ✅ | ✅ | — |

**Khuyến nghị chạy:**

- **Đợt 1 (3 session song song):** P + T1 + C. Hoàn toàn rời file nhau.
- **Đợt 1.5:** R có thể chạy song song với đợt 1 trong worktree riêng —
  xung đột duy nhất là vài dòng trong `MainComponent.cpp` (P thêm seed +
  nút preset ở ctor; R thêm 1 lambda provider cũng ở ctor). Chấp nhận một
  lần rebase nhỏ: **merge P trước, R rebase sau** (conflict dự kiến ≤10
  dòng, cùng vùng ctor). Nếu muốn zero-conflict thì để R bắt đầu sau khi P
  merge.
- **T2** là buổi hẹn với người + rig: gom 3 việc một buổi (sweep thật,
  acceptance ring-risk, nghe clamp 1.0.4) — làm SAU khi R đã release alpha
  để rig có chip RING RISK sống mà thử.
- **Mỗi session release alpha riêng khi lane mình xong** — script tự bump
  patch nên không giẫm số nhau, nhưng **commit CMakeLists.txt ngay sau
  release** (luật sẵn có) để lane sau không bump từ số cũ. Hai lane KHÔNG
  release cùng lúc (drop folder + prune là shared resource — global rule 7).
- Worktree mới nhớ: `git submodule update --init --recursive` + copy
  `external/asiosdk/` + `installer/vendor/` từ checkout chính.
